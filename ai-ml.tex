% !TeX root = main.tex
\section{人工智能与机器学习}
\label{sec:aiml}

\subsection{图像特征}

机器视觉要解决的根本问题可以归结为两个问题：人类究竟依据哪些图像特征在视觉上识别出物体？这些信息又该如何表示，才能让计算机也具备识别物体的能力？

让计算机也具备识别物体的能力这个问题可以进一步拆成两个子任务。
\begin{itemize}
  \item 分类任务是把一次观测分配给某个类别；
  \item 学习任务则是学习出一组类别边界，使这种分配尽可能不出错。
\end{itemize}
机器视觉的处理流程在根本上分为两级：先由\textbf{特征生成（Merkmalsgenerierung）}把原始图像转换成一组具有描述能力的特征向量，再由\textbf{分类（Klassifikation）}依据这些特征把观测归入某个类别。前者决定了信息是否被有效提取，后者决定了决策边界如何划分。

对图像特征本身，有三点基本要求：可靠性（同一物体在不同条件下能被稳定识别）、可计算性（提取特征所需的计算开销要可控）、可区分性（不同类别的特征要能被明确区分开）。其中可靠性具体体现为特征应尽可能对以下影响保持\textbf{不变性}：物体在图像中的\textbf{大小}（尺度）、\textbf{平移与旋转}、场景\textbf{亮度}与光照条件、传感器\textbf{噪声}，以及观察\textbf{视角}的变化。现实中没有哪种特征能同时对所有影响完全不变，因此实际应用往往是在不变性和可区分性之间做权衡——这也正是 SIFT 等方法着力解决尺度与旋转不变性的原因。

图像特征大致可以分成两类：一类是\textbf{特定的强度变化}，可以通过\textbf{梯度大小}、\textbf{梯度方向}、\textbf{卷积操作（滤波器）}等方式量化计算；另一类是\textbf{图像局部区域内强度变化在特定方向上的分布}，常用方向直方图来描述。

在卷积神经网络普及之前，图像特征提取主要依赖两条技术路线：基于梯度的方法，以及基于统计分布的方法。

\subsection{卷积神经网络}

经典方法有一个共同的痛点：特征提取环节高度依赖人工设计和专家经验，需要反复尝试和评估不同的特征提取与分类方案，费时费力。卷积神经网络（CNN）最大的价值就在于\textbf{取消了基于人工的特征提取}，它被整合进分类模型内部，训练过程中自动完成，这种从原始图像直接到分类结果的方式被称为\textbf{端到端学习（End-to-End Learning）}。

如图 \ref{fig:cnn} 所示：CNN 的核心组件是卷积层：神经元按宽度、高度、深度三个维度排列，专门为图像处理设计。卷积层的参数由一组可学习的感受野滤波器（权重矩阵）组成，这些滤波器也被称为特征检测器，其宽高通常远小于输入图像，滤波器的数量对应输入图像的深度。在每个位置上，滤波器的各个通道与图像对应区域的通道逐元素相乘再求和，这个操作就是卷积；一个滤波器在整张图像上卷积得到的结果构成一张特征图（Feature Map），特征图中被突出显示的区域，就是与该滤波器最相似的区域。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{cnn.jpg}
  \caption{卷积神经网络}
  \label{fig:cnn}
\end{figure}

池化层通常夹在两个卷积层之间，作用是从前一卷积层得到的特征图中剔除对模式识别而言冗余的参数，从而降低计算量。通过多层卷积和池化的堆叠，网络可以构建出复杂的非线性映射关系：层与层之间形成一种层级结构，逐步实现自动特征提取，特征也随层数加深逐级浓缩。每个节点基于前一层的输出学习特定的特征——最初的几层通常只识别角点、边缘等简单模式，越往后的层则负责聚合和组合这些简单模式，从而识别出更复杂的形状和物体。

\subsection{图像内容分类}

\paragraph{贝叶斯分类器（Bayes Classifier）}

经典分类方法中，贝叶斯分类器基于贝叶斯定理
\begin{equation}
    P(c|x) = \frac{P(x|c)\cdot P(c)}{P(x)}
\end{equation}
对给定的观测 \(x\)，选择使后验概率 \(P(c|x)\) 最大的类别作为判断结果。它的优点是模型简单、易于实现，且所需的训练数据量较少。

\paragraph{k 近邻（k-Nearest-Neighbor）}

k 近邻（k-Nearest-Neighbor）走的是一条更直观的路线，如图 \ref{fig:knn} 所示：判断一个未知物体属于哪一类，依据是它和已知物体之间的相似度，相似度通过距离度量转换成具体数值来表达。优点是分类器直观、实现简单，同时又能够拟合比较复杂的类别边界。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.4\textwidth]{knn.png}
  \caption{k 近邻分类器}
  \label{fig:knn}
\end{figure}

\paragraph{支持向量机（Support Vector Machine）}

如图 \ref{fig:svm} 所示，支持向量机（SVM）的核心思路是用一个超平面把线性可分的数据分开，并让训练样本（支持向量）到分界面的正交距离尽可能大；对于线性不可分的数据，可以通过映射到更高维的特征空间来处理，这就是常说的核技巧（Kernel-Trick）。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.4\textwidth]{svm.png}
  \caption{支持向量机}
  \label{fig:svm}
\end{figure}

\paragraph{神经网络（Neural Network）}

神经网络的基本结构是：输入值通常写成向量 \(x=(x_1, x_2, \ldots, x_m)\)，每个输入对应一个网络权重 \(w_i\)，网络活性 net 由输入与权重的加权和计算得出，再经过一个激活函数 \(f(x)\) 得到输出。可以调节的超参数包括网络架构或拓扑结构（神经元数量、层数、连接方式）、激活函数的类型（阈值函数、Sigmoid 函数等）、训练过程中权重的调整方式（反向传播算法），以及阈值的调整方式。

用CNN做分类时，网络末端通常接若干全连接层（Fully Connected Layers，FC）。倒数第二个全连接层输出一个长度为 \(k\) 的向量，\(k\) 等于类别数量；对这一层应用 Softmax 激活函数，计算出每个类别对应的概率，所有类别的概率加起来正好等于 1。